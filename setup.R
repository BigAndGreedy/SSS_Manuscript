# Importing files/loading packages

library(dplyr)
library(tidyr)
library(purrr)

library(ggplot2)
library(gt)

library(FSA)

# file import

westMN = read.csv("./files/westMN.csv")
jamestown <- read.csv("./files/jamestown.csv")
